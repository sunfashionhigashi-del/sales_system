# Outlook連携・メール案件管理自動化（M365非依存）実装プラン

## 1. 目標（ユーザー要望の整理）

M365（Microsoft 365）を前提にせず、独自ドメイン/他社プロバイダで運用しているメールサーバーを使いながら、
Outlookを日常のメーラーとして継続利用し、以下をワンストップで実現する。

- 受信メールの自動分析
- メールを案件単位に自動ひも付け
- 返信漏れ/対応遅延のアラート
- 返信ドラフトの自動生成
- IT専門家でなくても使える、わかりやすいUI

---

## 2. 全体アーキテクチャ（非M365前提）

### 2.1 採用方針

- **メール取得は標準プロトコル（IMAP/SMTP）ベース**にする。
- Outlookは「閲覧・送受信のフロント」として使い続ける。
- 自動化ロジックはクラウド/サーバー側の別システムで実行する。

### 2.2 構成要素

1. **メール取り込みサービス**
   - IMAP IDLEまたは定期ポーリングで受信メールを取得
   - 添付ファイル（PDF/Excelなど）も抽出
2. **案件判定エンジン**
   - 件名、送信元、本文、添付名、本文内キーワードから案件候補を特定
   - 過去の同一スレッド・同一企業履歴も利用
3. **タスク/期限管理エンジン**
   - 「要返信」「確認待ち」「保留」などに自動分類
   - SLA（例: 24時間以内初回返信）を超えたらアラート
4. **ドラフト生成AI**
   - 日本語ビジネス文面の返信案を生成
   - 口調テンプレート（丁寧/簡潔）や社内定型文を反映
5. **オペレーションUI（Web）**
   - ノーコード寄りの操作画面
   - 「案件一覧」「要返信」「期限超過」「ドラフト確認」を一画面で扱う
6. **Outlook連携レイヤ**
   - Outlookに案件IDカテゴリを付与（任意）
   - 承認済みドラフトをSMTPまたはOutlook下書きとして戻す

---

## 3. Outlookを継続利用する運用設計

- 日常業務はOutlookで継続。
- 自動化システムは裏側で同じメールボックスを監視。
- 担当者は必要時のみWeb画面を開き、以下だけ確認する。
  - 返信漏れアラート
  - AIドラフト候補
  - 案件とのひも付け結果

> これにより「Outlookはそのまま」「難しい操作は増やさない」を両立する。

---

## 4. IT非専門家向けUI要件（最優先）

### 4.1 画面の基本原則

- 1画面1目的（情報過多を避ける）
- 専門用語を排除（例: SLAではなく「返信期限」）
- 色の意味を固定
  - 赤: 期限超過
  - 黄: 今日中に返信
  - 緑: 対応済み

### 4.2 最低限の主要画面

1. **今日の対応画面**
   - 「本日返信が必要」だけを上部表示
2. **案件タイムライン画面**
   - 受信→対応→送信の時系列表示
3. **返信ドラフト確認画面**
   - AI案（左）/最終送信文（右）比較
   - 「採用」「修正」「却下」3ボタンのみ
4. **設定画面（簡易）**
   - 返信期限（時間）
   - 通知先（メール/Teams/Slack）
   - 会社定型文テンプレート

---

## 5. 自動化ロジックの実装指針

### 5.1 案件ひも付け

- 優先順位付きルールで判定
  1. Message-ID / In-Reply-To
  2. 件名類似（Re/Fwd除去）
  3. 送信元ドメイン + 担当者
  4. 添付ファイル名や注文番号
- 信頼度スコアが閾値未満なら「要確認」に回す。

### 5.2 返信漏れ検知

- 「要返信」判定条件を明文化
  - 顧客からの質問
  - 見積依頼
  - 納期確認依頼
- 最終受信時刻からの経過で段階アラート
  - 12時間: 注意
  - 24時間: 警告
  - 48時間: エスカレーション

### 5.3 ドラフト生成

- 入力情報
  - 最新メール本文
  - 案件履歴
  - 製品・価格・納期マスタ
- 出力テンプレート
  - 件名案
  - 本文案
  - 不明点（AIが確認したい事項）

---

## 6. 導入ステップ（失敗しにくい順）

### Phase 1: 可視化

- 受信メールの自動取り込み
- 案件一覧と要返信一覧を表示
- 返信漏れアラートのみ先行導入

### Phase 2: 半自動化

- 案件自動ひも付け
- ドラフト生成（人間承認必須）

### Phase 3: 高度化

- 添付OCR/帳票解析
- 顧客ごとの文体最適化
- KPIダッシュボード（返信時間短縮率など）

---

## 7. 推奨技術スタック（実装しやすさ重視）

- Backend: Python (FastAPI) または Node.js (NestJS)
- Mail: IMAP/SMTP（プロバイダ標準）
- DB: PostgreSQL
- Queue: Redis + ワーカー（RQ/Celery/BullMQ）
- Frontend: React + シンプルな管理UI
- AI: LLM API（ドラフト生成・分類）

※ Outlook依存機能を最小にすることで、将来的にメーラー変更しても中核ロジックを再利用可能。

---

## 8. セキュリティ・運用上の注意

- メール接続情報はシークレット管理（平文保存禁止）
- 添付ファイルは暗号化保存
- AI送信前に個人情報マスキング設定を用意
- すべてのドラフト生成/送信操作を監査ログ化

---

## 9. 成果指標（導入効果の測定）

- 返信漏れ件数（導入前後）
- 初回返信までの平均時間
- 担当者の手動振り分け時間
- AIドラフト採用率

---

## 10. 非IT担当者向けの最終運用イメージ

- 朝: 「今日の対応画面」を開く
- 昼: 赤/黄案件だけ処理
- 返信時: AIドラフトを必要箇所だけ修正して送信
- 夕: 期限超過ゼロを確認

この運用を定着させることで、メール対応を「個人依存」から「仕組み化」へ移行できる。
